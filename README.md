A cute little tool for visualizing option bundles. Enter a stock into the search bar, then add BUY and SELL a couple of options to see what your payout will be for that bundle at different prices.

To run:

pip install flask
python hello.py
